{{#if message}}
<div class="alert alert-{{message.type}} alert-dismissible fade show" role="alert">
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    <strong>{{message.intro}}: </strong> {{message.message}}
</div>
{{/if}}
<div class="admin-home-container">
    <div class="admin-racharge-table-container">
        <h3 style="color: white">Danh sách tài khoản bị khóa vô thời thạn</h3>
        <table id="admin-racharge-table" class="admin-racharge-table table">
            <thead>
                <tr>
                    <th scope="col">Tài khoản</th>
                    <th scope="col">Chủ tài khoản</th>
                    <th scope="col">Thời gian bị khóa</th>
                    <th scope="col">Bị khóa vô thời hạn</th>
                </tr>
            </thead>
            <tbody>
                {{#each data}}
                <tr>
                    <th scope="row">{{username}}</th>
                    <td>{{fullname}}</td>
                    <td>{{last_update}}</td>
                    <td>{{locked}}</td>
                    <td>
                        <a href="/admin/accounts/locked/{{_id}}">Chi tiết</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>